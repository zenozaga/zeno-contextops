# Generate Docker Compose

**Description:**
Create a docker-compose.yml file for local development with all selected services (DB, cache, event broker).

## Steps
1. Add services for database, cache, event broker
2. Configure environment variables
3. Validate service startup

## Outcome
- Local development environment ready
