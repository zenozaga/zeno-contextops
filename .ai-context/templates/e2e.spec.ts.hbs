import { describe, it, expect } from "vitest";
// import { {{name}} } from "@/adapters/{{slug}}";

describe("{{name}} E2E", () => {
  it("should create a customer", async () => {
    expect(true).toBe(true);
  });
});
